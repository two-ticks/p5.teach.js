//TODO : transform function : use morphing

import * as config from "../config.js";

export function transform(_object_init: any, _object_finl: any) {

  
console.log(config.hello[0]);

  if (_object_init.writeTexElement && _object_finl.writeTexElement) {
    //TeX transformation
    
  } else if (_object_init.writeTextElement && _object_finl.writeTextElement) {
  }
}
